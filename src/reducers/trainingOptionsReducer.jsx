const trainingOptionsReducer = (state = [], action) => {
    switch (action.type) {
        case "send training options":
            return state;
        default:
            return state;
    }
}

export default trainingOptionsReducer
